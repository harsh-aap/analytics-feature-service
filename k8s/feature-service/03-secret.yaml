# ── Secret — feature-service sensitive values ─────────────
# DO NOT commit real values. Use External Secrets Operator in prod.
#
# Quick-start (dev/staging only):
#   kubectl create secret generic feature-service-secrets \
#     --namespace analytics \
#     --from-literal=MONGO_HOST="your-docdb-or-atlas-endpoint" \
#     --from-literal=MONGO_USER="feature_svc" \
#     --from-literal=MONGO_PASSWORD="REPLACE" \
#     --from-literal=REDIS_HOST="your-elasticache-endpoint" \
#     --from-literal=REDIS_PASSWORD=""
#
# Note: no KAFKA_BROKERS — MSK auth uses KAFKA_CLUSTER_ARN (IRSA) from configmap.
apiVersion: v1
kind: Secret
metadata:
  name: feature-service-secrets
  namespace: analytics
type: Opaque
stringData:
  MONGO_HOST:     "analytics-feature-docdb.cluster-cnm2acuayntt.ap-south-1.docdb.amazonaws.com"
  MONGO_USER:     "featureadmin"
  MONGO_PASSWORD: "1bs14icpuIGgmhBh7uLbq2pb"
  REDIS_HOST:     "REPLACE_WITH_REDIS_HOST"
  REDIS_PASSWORD: ""
